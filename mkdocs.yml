site_name: WBData
theme: readthedocs
plugins:
  - mkdocstrings:
      default_handler: python
      handlers:
        python:
          options:
            show_source: false
            members_order: source
            docstring_options:
              returns_named_value: false
              returns_multiple_items: false
watch:
  - wbdata
